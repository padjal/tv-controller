/target
.idea/
# Claude Code local settings and its worktree checkouts.
.claude/
.env
*.db
*.db-wal
*.db-shm

# dashboard
node_modules/
dist/
# vite build output, served by the server at runtime
server/dashboard/

# Video library and container data live here but are not part of the repo.
/videos/*
!/videos/.gitkeep
/data/
# Pre-transcode originals kept by scripts/transcode.sh. Large, and the Pis
# never read them — only the delivery encodes in /videos are served.
/masters/

# macOS
.DS_Store
